[project]
name = "fastapi-sample"
version = "0.1.0"
description = "FastAPI sample application for lttle.cloud"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["fastapi>=0.120.1", "uvicorn>=0.38.0"]
